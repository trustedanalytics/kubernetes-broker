{
  "kind": "ReplicationController",
  "apiVersion": "v1",
  "metadata": {
    "name": "$idx_and_short_serviceid-slave-1",
    "labels": {
      "org": "$org",
      "space": "$space",
      "catalog_service_id": "$catalog_service_id",
      "catalog_plan_id": "$catalog_plan_id",
      "service_id": "$service_id",
      "idx_and_short_serviceid": "$idx_and_short_serviceid-slave-1",
      "managed_by": "TAP"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "service_id": "$service_id",
      "idx_and_short_serviceid": "$idx_and_short_serviceid-slave-1"
    },
    "template": {
      "metadata": {
        "labels": {
          "service_id": "$service_id",
          "idx_and_short_serviceid": "$idx_and_short_serviceid-slave-1",
          "managed_by": "TAP"
        }
      },
      "spec": {
        "volumes": [
          {
            "name": "postgresql-credentials",
            "secret": {
              "secretName": "$idx_and_short_serviceid-postgresql-credentials"
            }
          }
        ],
        "serviceAccountName": "$idx_and_short_serviceid-slave-1",
        "containers": [
          {
            "name": "k-postgresql94",
            "image": "sameersbn/postgresql:9.4-20",
            "command": ["/bin/bash"],
            "args": ["-c", "export MASTER_HOST_NAME=${MASTER_HOST_NAME^^} && export REPLICATION_HOST=${!MASTER_HOST_NAME} && export PGPASSWORD=${REPLICATION_PASS} && while true ; do  ERR=`psql -h ${REPLICATION_HOST} -U ${REPLICATION_USER} -c 'SELECT 1' -d postgres  2>&1 >/dev/null` ; [[ ${ERR} != '' ]] || break ; echo 'Waiting for master ...' && sleep 5; done && /sbin/entrypoint.sh "],
            "ports": [
              {
                "containerPort": 5432,
                "protocol": "TCP"
              }
            ],
            "env": [
              {
                "name": "REPLICATION_USER",
                "valueFrom": {
                  "secretKeyRef": {
                    "name": "$idx_and_short_serviceid-postgresql-credentials",
                    "key": "replication-user"
                  }
                }
              },
              {
                "name": "REPLICATION_PASS",
                "valueFrom": {
                  "secretKeyRef": {
                    "name": "$idx_and_short_serviceid-postgresql-credentials",
                    "key": "replication-pass"
                  }
                }
              },
              {
                "name": "DB_USER",
                "valueFrom": {
                  "secretKeyRef": {
                    "name": "$idx_and_short_serviceid-postgresql-credentials",
                    "key": "db-user"
                  }
                }
              },
              {
                "name": "DB_PASS",
                "valueFrom": {
                  "secretKeyRef": {
                    "name": "$idx_and_short_serviceid-postgresql-credentials",
                    "key": "db-pass"
                  }
                }
              },
              {
                "name": "DB_NAME",
                "valueFrom": {
                  "secretKeyRef": {
                    "name": "$idx_and_short_serviceid-postgresql-credentials",
                    "key": "db-name"
                  }
                }
              },
              { "name": "PG_TRUST_LOCALNET", "value": "true"},
              { "name": "REPLICATION_MODE", "value": "slave"},
              { "name": "REPLICATION_SSLMODE", "value": "prefer"},
              { "name": "MASTER_HOST_NAME", "value": "$idx_and_short_serviceid_MASTER_SERVICE_HOST"},
              { "name": "REPLICATION_HOST", "value": ""},
              { "name": "REPLICATION_PORT", "value": "5432"},
              { "name": "MANAGED_BY", "value":"TAP" }
            ],
            "imagePullPolicy": "IfNotPresent"
          }
        ],
        "restartPolicy": "Always",
        "dnsPolicy": "ClusterFirst"
      }
    }
  }
}